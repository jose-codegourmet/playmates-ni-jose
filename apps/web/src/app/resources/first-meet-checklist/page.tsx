import { PlaceholderPage, placeholderMetadata } from "@/sections/placeholder/PlaceholderPage";

export const metadata = placeholderMetadata("firstMeetChecklist");

export default function FirstMeetChecklistPage() {
  return <PlaceholderPage page="firstMeetChecklist" />;
}
